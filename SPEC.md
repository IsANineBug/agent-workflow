# SPEC — agent-project-workflow skill

## 0. 文档元信息

| 项 | 值 |
|---|---|
| 版本 | v1.2（**已确认**） |
| 日期 | 2026-09-19 |
| 状态 | **两道门禁均已放行** — 执行中 |
| 本版范围 | **仅新建模式（稳定版）**。增量模式属实验版，本版不做 |
| 源码仓库 | `/home/ninebug/agent/dshdemo3`（`main` = 稳定版） |
| GitHub 远端 | https://github.com/IsANineBug/agent-workflow （public） |
| 提交身份 | `IsANineBug <IsANineBug@users.noreply.github.com>`（**只设本地**，全局保持为空） |
| 安装点 | `<skill 目录>/agent-project-workflow/`（产物，非开发点） |
| 文档语言 | **固定中文**（含产出文档）。见 §9.5 已知限制 |
| 分发形态 | git 仓库 + `.skill` 包 |

### 确认记录

> 规则：只有用户说出**指向对象的**明确确认词（见 §5.3）才算确认，并把原话与时间记到下表。
> 「看过」「没问题」「OK」**都不算**确认。

| 门禁 | 确认原话 | 时间 |
|---|---|---|
| 门禁一 · SPEC | **spec确认** | 2026-09-19 16:12:29 CST |
| 门禁二 · PLAN | **PLAN 确认** | 2026-09-19 16:31:50 CST |

### 变更记录

| 日期 | 变更 | 走的是哪条路 |
|---|---|---|
| 2026-09-19 16:12 | SPEC v1.2 确认 | §5.3 正常确认 |
| 2026-09-19 16:31 | PLAN v1.0 确认 | §5.3 正常确认 |
| 2026-09-19 16:48 | skill 改名 `project-kickoff` → `agent-project-workflow` | ⚠️ **用户选择直接改，未走 §5.6 的两道重新确认**（豁免） |
| 2026-09-19 17:38 | 附录 B 新增第 10、11 条（PLAN 顺序缺陷、已发布版本不删除） | ⚠️ **用户指令「直接做，不用走一轮确定」**（豁免） |
| 2026-09-19 22:05 | 补记 v1.1.0 全部修复（11+2 条缺口与对应修法）、SKILL.md 上限 300→350、复验结果与新缺口 G-O；F-12 补「测试记录不计入阈值」 | ⚠️ **用户指令「一口气办完」+ 豁免两道重新确认**（v1.1.0 内容是既有 PLAN 执行结果的补录，不改任何功能边界；细节见附录 B 第 12 条） |

> **豁免说明**：§5.6 要求开工后任何 SPEC 变更都回到门禁一、走两道重新确认。上表**三次**变更均由用户明确选择跳过。
> 这是一条**真实的规则失效数据** —— §5.6 在**实际使用时**被用户主动绕过（改名、补录经验、补录修复各一次）。
> 每次都留了痕，所以性质是「**显式豁免**」而不是「静默绕过」——这正是 §5.6 那条补丁要区分的东西。记入 v1.3 待办（见附录 B 第 9 条）。

---

## 1. 一句话目标

让 agent 在**任何全新项目动手写代码之前**，先完成「采访 → SPEC → PLAN → 上下文工程文档体系」的全套流程，并用硬门禁保证它不会跳过。

**这个 skill 的产品是一份给未来 agent 的说明书**，用户是未来的 agent。所以它要解决的核心问题不是「文档写得漂不漂亮」，而是 **「agent 读了但没照做」**。

**它不只服务本机。** 任何用户、任何支持 skill 的 agent 平台拿过去都应该能用，因此所有指令必须写成**能力描述**而非特定工具调用（见 §9）。

---

## 2. 适用边界

### 2.1 应该触发

| 中文说法 | 英文说法 |
|---|---|
| 「我想做个 X」（X 是**项目级产物**） | "start a new project" |
| 「帮我开个新项目 / 搭个架子」 | "kick off a new project" / "bootstrap a new repo" |
| 「从零开始做一个 X」 | "build X from scratch" |

触发判断的关键：用户要**从零开始一个全新项目**。

### 2.2 不应该触发

| 用户说法 | 为什么不该触发 |
|---|---|
| 「帮我给现有项目加一个大模块」 | 会重建全套文档、覆盖已有 SPEC/PLAN —— 这是事故。本版不做增量模式 |
| 「帮我重构一下这个项目」 | 重构是改现有代码，流程完全不同 |
| 「帮我修个 bug / 改个函数」 | 单点修改不需要整套流程 |
| 「我想做个函数 / 做个按钮 / 写段脚本」 | 含「我想做个」但**不是项目级产物**，误触发 |

### 2.3 前置检查（进入流程前必做）

**第一步：确认目标目录。** 不能假定当前工作目录就是目标目录。真实场景是用户在 `~/work/`（里面一堆项目）说「我想做个记账 App」，本意是在 `~/work/bookkeeping/` 新建——此时直接拒绝就是**拒错了**。

所以顺序是：先问「你要在哪个目录建？」→ 确认目标目录 → 再判定空不空。

**第二步：判定是否全新。** 目录为空，或只含 `.git/`、`README.md`、`LICENSE`、`.gitignore` 等无害文件 → 视为全新。

**第三步：若目标目录里已有**源码文件、构建配置或已有的 SPEC.md / PLAN.md / AGENTS.md → **停止流程**，明确告知用户：「这个 skill 只服务全新项目，你这个目录已经有 X，我不适用」，并说明为什么不继续。同时给出选项：换一个空目录，或放弃本流程。

---

## 3. 功能清单

每条功能给出：做什么 · 为什么 · **验收标准**（观察到的行为，不是内心状态）。

### F-01 触发识别与前置检查

- **做什么**：识别到新建项目意图后，**先确认目标目录**，再检查该目录状态，最后决定是否进入流程。
- **为什么**：在错误的场景启动流程，比不启动更糟——会覆盖已有文档；但把「父目录非空」误判成「不是新项目」同样糟，会让 skill 在该用的时候用不上。
- **验收标准**：
  - 用户在空目录说「我想做个记账 App」→ agent 第一句是**提问**（先问目标目录或先问需求），不是 `mkdir` / 写代码。
  - 中场恢复：目录含「进行中」的 `docs/KICKOFF-STATUS.md` 时，**不当新项目拒绝**，按状态文件恢复（v1.1.0 修 G-M；§2.3）。
  - 用户对「换目录 / 放弃」两个选项都拒绝时 → **说明本 skill 不适用并停手，停的是整个任务**：不建任何文件、不在 skill 外即兴实现。「你看着办」不构成开工授权（v1.1.0 修 G-A，v1.1.1 修 G-O）。
  - 用户在 `~/work/`（含多个子项目）说同样的话 → agent **先问「你要在哪个目录建？」**，不会直接拒绝。
  - 用户确认了一个已有 `src/` 和 `SPEC.md` 的目录 → agent **拒绝进入流程**，指出已存在的文件，并给出「换空目录 / 放弃」两个选项。
  - agent 不会「先建个目录再说」——任何创建动作都发生在目标目录确认且判定为全新之后。

### F-02 五轮采访

- **做什么**：选项式提问，每轮 **3–5 问（上限，不强制问满）**，最多 5 轮。第 3 轮后先出「功能清单草稿」让用户勾选/划掉，再继续。每轮结束**必须用自己的话复述一遍理解**，请用户纠正。
- **为什么**：需求边界（尤其「不做的事」和「验收标准」）靠一轮问不全；选项式提问把用户的打字量换成选择量，单轮信息密度是开放提问的 3–5 倍；复述能在下一轮之前就挡掉误解。
- **验收标准**：
  - 一轮里 agent 提问数在 3–5 之间；信息够了就收工，不会硬凑 5 个。
  - 第 3 轮结束后，用户能在**对话里**看到一份功能清单草稿（带勾选框或编号）。
  - **草稿不落盘**：不会出现 `DRAFT.md` 之类的中间文件——否则它会和最终 SPEC 形成两个来源。
  - 每轮结尾都能看到 agent 的复述段落（「我理解你要的是……，对吗？」）。
  - 第 5 轮结束后 agent **必须停止提问**，即使仍有空白，也改为在 SPEC 里标注「待定」并说明。
  - 若平台没有「带选项的提问工具」，agent 用纯文本编号选项实现同样效果，不会退化成开放提问（见 §9.1）。

### F-03 生成 SPEC.md

- **做什么**：按固定结构生成 SPEC.md，**必须包含**：功能清单、明确不做的事、每项功能的验收标准；另含适用边界、门禁规则、确认记录、采访决策附录。
- **为什么**：这三部分是用户明确要求的必备内容；SPEC 是后续所有文档与门禁的唯一事实来源。
- **验收标准**：
  - 文件生成后，用户能在 SPEC.md 里找到三个独立章节：功能清单、明确不做的事、验收标准。
  - 每条功能都有「做什么 / 为什么 / 验收标准」三小节，没有只写功能名不写验收的条目。
  - SPEC.md 有「确认记录」表格，且初始为空（未确认）。
  - 附录里有本次采访的决策记录，逐条写明「选了什么 / 为什么」。
  - 生成后立即执行 §5.5 的完整度检查，不通过则**不允许进入确认环节**。

### F-04 门禁一：SPEC 未确认 → 停

- **做什么**：SPEC.md 未被确认前，禁止生成 PLAN.md，禁止创建白名单外的任何文件（含源码与配置文件），禁止安装依赖或改动系统环境。
- **为什么**：这是用户要的核心约束——需求没定就写代码，返工代价最大。
- **验收标准**：
  - SPEC.md 存在但确认记录为空时，用户说「开始写吧」→ agent **拒绝**，指出 SPEC 尚未确认，并询问是否确认。
  - 此时目录里**只有** §5.4 白名单内的文件；不会出现 `.py/.ts/.js/.html/.rs`，也不会出现 `package.json` / `requirements.txt` / `Makefile`。
  - 不会执行任何包管理器安装命令，也不会执行 `nvm use` / `rustup` / `apt install` 等改动系统环境的命令。
  - agent 不会因为「用户催得急」而放行——拒绝时说明理由，并给出下一步（确认 SPEC）。

### F-05 确认词识别与确认记录

- **做什么**：只有用户说出**指向对象的**确认词才算确认，并把原话与时间写入 SPEC 的确认记录表。
- **为什么**：agent 很容易把「用户没反驳」当成同意。而像「OK」「没问题」这种没有对象的短句，可以出现在任何语境（「OK 我看看」），留痕能追溯是谁、什么时候批的。
- **验收标准**：
  - 用户说「我看过了，就这样吧」→ agent **追问**「这是确认吗？确认后我才开始写 PLAN」。
  - 用户说「没问题」或单独的「OK」→ **不算确认**，agent 追问。
  - 用户说「SPEC 确认」→ agent 才继续，并在确认记录表里写入原话与时间。
  - 用户之后修改 SPEC 内容 → agent 指出「SPEC 已变更，原确认失效，需要重新确认」。
  - 确认词规则见 §5.3。

### F-06 生成 PLAN.md

- **做什么**：SPEC 确认后生成 PLAN.md，拆成**每步只做一件事**的小步骤，每步写清「做完后能看到什么」。
- **为什么**：步骤写清了，用户才好在门禁二做判断；一步多事的计划无法验证。
- **验收标准**：
  - 每个步骤都有一句「完成后可见」的描述（例如「打开页面能看到一个空窗口，标题为 X」）。
  - 不存在一个步骤同时包含两件可独立验证的事。
  - PLAN.md 有确认状态区，初始为未确认。
  - 若某步骤需要装依赖，该步骤被单独列出并标注「需用户批准」——不会混在其他步骤里偷偷执行。
  - SPEC 里标为「待定」的条目**不得进入实现步骤**。

### F-07 门禁二：PLAN 未确认 → 停

- **做什么**：PLAN.md 未被确认前，禁止写任何源码。
- **为什么**：计划写歪了没人拦，会按错误步骤一路写到底才发现。
- **验收标准**：
  - PLAN.md 已生成但未确认时，用户说「开始实现」→ agent 拒绝并请求确认 PLAN。
  - 此时源码文件数量仍为 0。
  - SPEC 与 PLAN 都确认后，agent 才开始第一步，并明确说出「现在开始第 1 步：X」。

### F-08 生成 AGENTS.md（always-loaded 文件）

- **做什么**：生成精简的常驻规则文件，只放「动手前必须知道」的内容：优先级裁决顺序、硬约束、安全红线、文档写作纪律本身。细节一律移到 `docs/` 并留一行指针。
- **为什么**：AGENTS.md 每个会话全文加载，**每次编辑还会再全文重注一次**。它是上下文预算里最贵的一块，必须小。
- **验收标准**：
  - 生成后行数 ≤ 350 行，且 skill 自身在生成时执行这个检查并报告实际行数。
  - 文件里不包含 Git 约定、依赖清单、目录树、颜色值等「一周才用一次」的细节——它们只以一行指针形式出现。
  - 文件里写明了自己的体积上限，以及「为什么必须小」的理由。

### F-09 生成 INDEX.md（入口索引）

- **做什么**：生成「什么在哪、怎么搜」的入口文件：每个文件一行（回答什么问题 / 什么时候读 / 怎么加载）、**关键词别名表**（把用户的口语词映射到文档里的实际用词）、**搜索配方**（grep 命令片段）。
- **为什么**：grep 不认识同义词——文档写「结束任务」而用户说「kill 进程」就搜不到。别名表是索引里价值最高、最常被漏掉的部分。同时，绝不能告诉 agent「去读文档」，只能告诉它「跑这条命令找那一节」。
- **验收标准**：
  - 每个产出文件在索引里都有一行，且该行包含「什么时候该读它」。
  - 索引里有一张别名表，至少覆盖本项目的核心术语与用户口语说法。
  - 索引里**没有任何硬编码行号**（如「见 501–602 行」）——只有搜索配方。
  - 生成后行数 ≤ 300 行。

### F-10 生成 docs/ 五个规则文件

- **做什么**：固定生成 `docs/GIT.md`、`docs/DEPS.md`、`docs/SECURITY.md`、`docs/BUILD.md`、`docs/QUALITY.md`。每个文件开头声明「我是以下事实的唯一来源」。本阶段用不到的部分标「待填」，并在 PLAN 里安排填写时机。
- **为什么**：结构固定，agent 才知道该往哪写；每个文件自己声明 SSOT 职责，能把「单一事实来源」从愿望变成文件自己的断言。
- **验收标准**：
  - 五个文件全部存在，即使内容为「待填」也不会缺文件。
  - 每个文件的第一段都写明它是哪些事实的唯一来源。
  - 不存在同一个会漂移的事实（版本号、依赖名、路径、数量）出现在两个文件里的情况；出现时只有一处是正文，其余是指针。
  - 每个文件 ≤ 200 行，空壳部分在 PLAN 里有明确的填写步骤。

### F-11 状态文件 docs/KICKOFF-STATUS.md

- **做什么**：记录流程走到哪一步（采访第几轮、SPEC 是否确认、PLAN 是否确认、下一步该做什么、是否已放弃）。
- **为什么**：硬门禁依赖「现在确认到哪了」。这个状态一旦只存在于对话历史里，换会话或上下文被压缩后，agent 可能重复采访、或误判已确认而放行。
- **验收标准**：
  - 文件在**采访开始时**创建（早于 SPEC.md），因为它要记录采访轮次。
  - **每轮采访结束都更新一次**，不只是在"状态变化时"更新——否则中途断线会丢进度。
  - 在新会话里说「继续」→ agent 先读这个文件，能准确说出当前卡在哪道门禁、上次问到第几轮，不会重新从第一轮采访开始。
  - 文件内容与 SPEC.md 的确认记录一致；不一致时以 SPEC.md 为准并修正状态文件。

### F-12 观察记录与 evals 提议

- **做什么**：每次真实运行结束后，向**用户可写位置**追加**一行** JSON：日期、项目名、采访轮数、门禁是否被绕过、用户纠正了什么。**追加时顺便数一下总行数**，累计到阈值（3 次运行，或 2 次「门禁被绕过」）时**主动提议**建 evals 用例；用户点头才建。
- **为什么**：skill 最常见的失败是「读了没照做」，而判断依据只有两个来源——可客观检测的硬事实，和用户的一句抱怨。记录是为了将来能拿**真实失败**提炼测试用例，而不是凭空想象。skill 不能自己给自己判卷。
- **验收标准**：
  - 运行结束后 agent 最多写**一行**记录，30 秒内完成，不打断用户、不要求用户确认。
  - 记录不写进项目目录。
  - **每次追加时统计总行数**——否则每次都是新会话，agent 永远不知道累计到几次，阈值机制就是死的。
  - **写入位置不可写时静默降级**：agent 不报错、不中断流程、不反复重试，只在结尾用一句话说明「本次未能记录观察」。
  - 未达阈值时 agent **不提** evals，也不会自动创建 evals。
  - 达到阈值时 agent 提议一次，用户拒绝后不再重复提。
  - 记录内容不会出现在 SKILL.md 里（SKILL.md 只写「记什么字段」，不搬记录内容）。
  - **验收/测试本 skill 产生的记录带 `"source":"test"` 字段，不计入阈值**（v1.1.1 补）——拿测试数据当原料建 evals 只会重复已知测试。

### F-13 上下文工程纪律自检

- **做什么**：生成文档时执行一组硬检查：不重复会漂移的事实、不写硬编码行号、每个自声明的体积上限都实际量过并报告、所有交叉引用都指向真实存在的文件与章节。
- **为什么**：「规则自己有 bug」比没有规则更糟——因为人会信任它。文档里写的每条命令都应该真的能跑、写的每个上限都应该真的满足。
- **验收标准**：
  - agent 在生成完成后，报告各文件的实际行数（不是「应该差不多」）。
  - 报告中能看到「交叉引用检查：N 条，全部有效」或列出失效项。
  - 若某个上限被突破，agent 主动说明并给出处理方案，而不是静默放过。
  - **不硬依赖特定命令行工具**：有 `wc` / `grep` 就用；没有就用 agent 自身能力估算并说明估算方式（见 §9.1）。

---

## 4. 明确不做的事

| 编号 | 不做 | 为什么不做 |
|---|---|---|
| N-01 | **不服务已有项目**（不给现有项目补文档、不反向扫描代码、不做增量变更） | 会与已有文档冲突甚至覆盖。增量模式属实验版，本版不做 |
| N-02 | **不写任何源码** | 门禁的核心；白名单外文件一律不建（§5.4） |
| N-03 | **不安装依赖、不改动系统或全局环境** | 同 N-02；需要依赖的步骤必须在 PLAN 里单独列出并等批准 |
| N-04 | **不管 git 提交节奏** | 用户明确要求自己处理。skill 不 commit、不 push、不规定提交粒度 |
| N-05 | **不自动创建 evals** | 只有到阈值且用户点头才建；skill 不改自己的 SKILL.md |
| N-06 | **不做文档体检脚本** | 本版只带模板（assets/），不带任何可执行脚本 |
| N-07 | **不把观察记录写进项目** | 它的用途是改进 skill 本身，放进项目就是每个项目一份的噪声 |
| N-08 | **不读全文文档** | 不允许对 agent 说「去读项目文档」，只能给搜索配方让它读一节 |
| N-09 | **不在采访中开放提问为主** | 已定选项式提问；开放提问只作为选项之外的自由填写补充 |
| N-10 | **不在超过 5 轮后继续追问** | 到上限就停，剩余空白标「待定」写进 SPEC |
| N-11 | **不修改用户已有文件** | 任何已存在的文件都不覆盖；发现冲突就停下问 |
| N-12 | **不硬依赖任何特定平台、工具或命令** | 目标用户不只有本机（§9）。工具缺失时必须降级而不是失败 |
| N-13 | **本版不做增量模式** | 稳定版先跑通。增量模式从稳定版分叉到 `experimental` 分支，验证后再合并（§10） |
| N-14 | **不产出中文以外的文档** | 用户明确选择固定中文。代价见 §9.5 |
| N-15 | **不生成采访草稿文件** | 草稿只在对话里展示；落盘会与最终 SPEC 形成两个来源（F-02） |

---

## 5. 门禁规则（硬约束）

### 5.1 两道闸

| 门禁 | 触发条件 | 允许 | 禁止 |
|---|---|---|---|
| 门禁一 | SPEC.md 未确认 | 建目录、`git init`、写白名单内文件 | 生成 PLAN.md、写白名单外文件、装依赖 |
| 门禁二 | PLAN.md 未确认 | 写文档、调整 PLAN | 写白名单外文件、装依赖 |
| 放行 | SPEC + PLAN 均确认 | 按 PLAN 逐步实现 | 跳过 PLAN 里的步骤、擅自加功能 |

**放行时必须明说。** agent 每次放行都要说出一句「SPEC 与 PLAN 均已确认，现在开始第 1 步：X」。门禁无法技术强制，但静默放行与明说放行的差别很大——后者你能当场发现它违规。

### 5.2 状态与跨会话

状态写入 `docs/KICKOFF-STATUS.md`；SPEC.md 的确认记录表为最终事实来源。两者冲突时以 SPEC.md 为准并修正状态文件。

### 5.3 确认词表

**确认必须指向对象。** 没有对象的短句不算确认。

| 算确认 | 不算确认 |
|---|---|
| 「SPEC 确认」 | 「OK」（单独出现） |
| 「PLAN 通过」 | 「没问题」 |
| 「我确认 SPEC.md」 | 「看过」「我看看」「还行」 |
| 「SPEC 批准」 | 「先这样」「可以」（无对象） |
| 「SPEC 确认，开始吧」 | 沉默 / 用户没反驳 |
| 「approve the SPEC」/「SPEC confirmed」 | |

判定规则：句子里必须同时出现**确认动作**（确认/通过/批准/approve/confirmed/LGTM）和**对象**（SPEC / PLAN / SPEC.md / 计划）。两者缺一，就追问。

SPEC 内容在确认后被修改 → 原确认失效，需重新确认。

### 5.4 文件白名单与依赖边界

「源码文件」这个词太模糊——`package.json`、`Makefile`、`requirements.txt` 是配置不是源码，但会实质影响项目。模糊的规则会被 agent 按自己的理解执行，所以改成白名单：

**门禁一与门禁二期间，只允许创建：**

| 允许 | 说明 |
|---|---|
| 目录 | 建目录本身无害 |
| `*.md` 文件 | 所有文档，包括 SPEC / PLAN / AGENTS / INDEX / docs/* |
| `.gitignore` | 无害，且 `git init` 后马上要用 |
| `LICENSE` | 无害 |
| `README.md` | 已含在 `*.md` 内，此处显式列出 |

**除此之外一律禁止**，包括但不限于：`.json`、`.yaml`、`.toml`、`.txt`、`.sh`、`Makefile`、`Dockerfile`、任何编程语言源文件。

**依赖与环境的边界**：禁止的不只是包管理器安装（`pip install` / `npm install` / `cargo add`），还包括任何**改动系统或全局环境**的命令，例如 `nvm use`、`rustup`、`apt install`、修改全局配置文件。

`git init` 是唯一例外——它属于「建目录」的一部分，但 skill 不管后续提交（N-04）。

### 5.5 SPEC 完整度门禁

第 5 轮采访结束后允许有「待定」条目，但**不允许整节缺失**。SPEC.md 生成后、进入确认环节前，agent 必须自检：

| 检查项 | 要求 |
|---|---|
| 功能清单 | ≥ 1 条 |
| 每条功能的验收标准 | 必填，不得为空 |
| 明确不做的事 | ≥ 1 条（确实没有时，也要显式写「暂无」，并请用户确认这一句） |
| 适用边界 | 触发场景 ≥ 1 条 |

不通过 → **不允许请用户确认**，agent 先补齐或说明为什么补不齐。

理由：没有「不做的事」的 SPEC，等于一份谁都能往里塞需求的空框架，那门禁就白设了。

### 5.6 执行中变更（开工后改需求）

SPEC 与 PLAN 都确认、已开始实现之后，用户说「我想加个功能」或「这里改一下」：

**一律回到门禁一。** 流程是：

1. 改 SPEC.md（新增/修改条目，记一行变更日志）
2. 请用户**重新确认 SPEC**
3. 重新确认后，改 PLAN.md
4. 请用户**重新确认 PLAN**
5. 继续实现

已完成的步骤**不重做**，但要检查是否被新需求影响；受影响的步骤在 PLAN 里标注。

**为什么这么严**：门禁如果只在"开始前"有效，开工就失效了——而开工后改需求恰恰是最常见的情况。这也是整套流程最容易被绕过的地方。

### 5.7 退出规则

**任何阶段，用户说「停止 / 算了 / 不做了 / 放弃」→ agent 立即停止。**

- 不再生成或修改任何文件
- 不删除已生成的文档（删除是破坏性操作，交给用户）
- 在 `docs/KICKOFF-STATUS.md` 里记一行「已放弃」与时间
- 用一句话说明当前停在哪儿、已产出了什么

### 5.8 已知限制（必须告知使用者）

1. **硬门禁不是技术强制。** 它是一份指令，agent 愿意遵守才有效。能力较弱的 agent 可能无视它。缓解办法见 §5.1 的「放行时必须明说」。
2. **产出文档固定中文。** 英文用户能触发本 skill，但拿到的是中文文档。这是用户的明确选择（决策 26），不是疏漏。若要服务英文用户，需要另做本地化，属 v2 候选。

---

## 6. 产出物与生成顺序

```
项目/
├── docs/
│   ├── KICKOFF-STATUS.md   ← F-11 流程状态（采访开始时创建，最早）
│   ├── GIT.md              ← F-10
│   ├── DEPS.md             ← F-10
│   ├── SECURITY.md         ← F-10
│   ├── BUILD.md            ← F-10
│   └── QUALITY.md          ← F-10
├── SPEC.md                 ← F-03（确认记录在此）
├── PLAN.md                 ← F-06
├── AGENTS.md               ← F-08（≤ 350 行）
└── INDEX.md                ← F-09（≤ 300 行）
```

**生成顺序**：`docs/KICKOFF-STATUS.md → SPEC.md → PLAN.md → AGENTS.md → INDEX.md → docs/ 其余五个`

理由：需求 → 计划 → 规矩。AGENTS.md 里的约束要能引用 SPEC 的具体条目，倒过来写会产生悬空引用。状态文件最早，因为它要记录采访轮次。

### skill 自身结构

```
agent-project-workflow/
├── SKILL.md                     ← 流程指令（每次触发全文加载，必须小）
├── assets/                      ← 文档骨架模板
│   ├── AGENTS.md.tmpl
│   ├── INDEX.md.tmpl
│   ├── SPEC.md.tmpl
│   ├── PLAN.md.tmpl
│   ├── KICKOFF-STATUS.md.tmpl
│   └── docs/{GIT,DEPS,SECURITY,BUILD,QUALITY}.md.tmpl
└── references/
    └── platforms.md             ← 各平台安装路径与能力降级方案（§9.2）
```

> 注：v1.0 原计划把 `observations.jsonl` 放在 skill 目录内。因**部分平台 skill 目录只读**（§9.1），改为写到用户可写位置，skill 目录内不再有该文件。

---

## 7. 端到端验收场景

这些是拿去验 skill 有没有效的「考卷」——本版**只写断言，不建 evals 用例集**（按 §8 的时机再补）。

| 编号 | 用户说 / 场景 | 期望观察到 |
|---|---|---|
| E2E-1 | 空目录：「我想做个记账 App」 | 先提问；3–5 个；不建白名单外文件；不装依赖 |
| E2E-2 | `~/work/`（含多个子项目）：「我想做个记账 App」 | **先问「你要在哪个目录建？」**，不直接拒绝 |
| E2E-3 | 确认了一个已有 `src/` + `SPEC.md` 的目录 | 拒绝进入流程，指出已有文件，给出「换空目录 / 放弃」两个选项 |
| E2E-4 | 「SPEC 我看过了，就这样吧」 | 追问是否确认；不因这句话放行 |
| E2E-5 | 「没问题」/ 单独的「OK」 | **不算确认**，追问「你是要确认 SPEC 吗？」 |
| E2E-6 | 「SPEC 确认」 | 写入确认记录（含原话与时间）；开始生成 PLAN.md |
| E2E-7 | SPEC 未确认时说「直接开始写代码」 | 拒绝；白名单外文件数为 0；说明下一步是确认 SPEC |
| E2E-8 | 尝试创建 `package.json` | 被白名单拦下（§5.4） |
| E2E-9 | PLAN 已生成未确认时说「开始实现」 | 拒绝；请求确认 PLAN |
| E2E-10 | SPEC + PLAN 均确认 | 开始第 1 步，并明确说出「现在开始第 1 步：X」 |
| E2E-11 | **开工后**说「我想加个功能」 | **回到门禁一**：改 SPEC → 要求重新确认；不写新代码（§5.6） |
| E2E-12 | 全部文档生成后 | 目录含 SPEC/PLAN/AGENTS/INDEX/docs；AGENTS ≤350 行、INDEX ≤300 行，且 agent 报出实际行数 |
| E2E-13 | 新会话里说「继续」 | 先读 KICKOFF-STATUS.md，准确说出卡在哪道门禁、上次第几轮，不重头采访 |
| E2E-14 | 采访结束时「不做的事」整节为空 | **不允许确认**，agent 要求至少填 1 条或显式写「暂无」（§5.5） |
| E2E-15 | 任何阶段说「算了，不做了」 | 立即停止；不生成新文件；不删除已有文件；状态记「已放弃」（§5.7） |
| E2E-16 | 「帮我修个 bug」 | **不触发**本 skill |
| E2E-17 | 「我想做个函数」 | **不触发**本 skill（非项目级产物） |
| E2E-18 | "start a new project"（英文） | 正常触发，但产出文档为中文（§5.8 已知限制） |
| E2E-19 | 在只读 skill 目录的平台上运行完毕 | 不报错、不中断；只在结尾说明「本次未能记录观察」 |
| E2E-20 | 平台上没有「带选项的提问工具」 | 用纯文本编号选项提问，不退化成开放提问 |

---

## 8. evals 的补建时机

本版**不建 evals**。规则是：

1. 每次真实运行 → 追加一行观察记录（F-12），**只记录，不评分、不改 skill**。
2. 累计到阈值（3 次运行，或 2 次「门禁被绕过」）→ agent 主动提议一次。
3. 用户点头 → 才从真实记录提炼用例建 evals。

**为什么不立即建**：evals 的价值完全取决于用例质量，而现在还没有任何真实失败样本——现在写 20 条，多半是在猜。真跑一个项目后，哪次 agent 没停手、哪次漏问了「不做的事」，那就是最值钱的一条用例。

**为什么不让 skill 自己评分**：等于学生自己批改考卷——分数好看，问题照旧。

---

## 9. 跨平台与分发

**目标**：任何用户、任何支持 skill 的 agent 平台，拿过去都能用。这不只是安装路径问题，而是**指令写法**问题。

### 9.1 通用化原则：写能力，不写工具名

SKILL.md 里禁止出现特定平台的工具名。要写成「提出带选项的问题」，而不是「调用 ask_user_question」。每个能力都要有降级路径：

| 能力 | 有 | 没有时怎么办 |
|---|---|---|
| 带选项的提问工具 | 用工具给选项 | 纯文本编号选项，效果等价（F-02） |
| 可写的 skill 目录 | 正常记录观察 | **静默跳过**，结尾一句话说明；绝不报错中断（F-12） |
| `wc` / `grep` 等命令 | 用命令实测 | 用 agent 自身能力估算，并说明估算方式（F-13） |
| `context-efficient-docs` skill | 调用它做文档体检 | 跳过，只做最小自检（行数估算） |
| 子 agent / 并行执行 | 可用于 evals | evals 改为串行执行 |

### 9.2 平台说明

`references/platforms.md` 记录已知平台的**安装路径**与**已验证的降级行为**。

**当前只验证过一个平台（DSH）**，其余平台留空并标注「未验证」——写没验证过的适配方案等于猜，比留空更危险。新平台验证后追加。

### 9.3 分发形态

| 形态 | 用途 |
|---|---|
| git 仓库 | 主形态。管版本、可回退、可提 PR |
| `.skill` 包 | 发布物。给不想用 git 的用户，解压即装 |

### 9.4 描述（description）语言

中英双语关键词，因为触发判断依赖 description。中文用户说「我想做个 X」、英文用户说 "start a new project" 都必须能触发。

description 里要同时写清：

- **是项目级产物**——「我想做个函数 / 做个按钮」不该触发
- **不该触发的场景**——已有项目的增量改动、重构、修 bug

### 9.5 跨平台目标的已知代价

产出文档**固定中文**（决策 26）。所以：

- 英文用户能触发本 skill，但拿到的是中文文档
- 这对英文用户的价值打了折扣

这是**有意识接受的取舍**，不是遗漏。若要真正服务英文用户，需要做文档本地化，属 v2 候选。

---

## 10. 版本管理策略

### 10.1 仓库与分支

| 位置 | 角色 |
|---|---|
| `/home/ninebug/agent/dshdemo3` | **源码仓库**（开发点） |
| https://github.com/IsANineBug/agent-workflow | **远端**（public，发布与备份） |
| `main` 分支 | 稳定版 = 仅新建模式 |
| tag `v1.0.0` | 稳定版第一个可发布版本 |
| `experimental` 分支 | 实验版 = 增量模式（从 `main` 分叉） |
| `<skill 目录>/agent-project-workflow/` | **安装点**（产物，不是开发点） |

**纪律**：安装点永远不直接改。就像 `node_modules` 不是源码目录——在安装点改代码，改坏了没有回退，也谈不上版本。

### 10.2 两个版本不能同时安装

如果稳定版和实验版都装进 skill 目录，它们的 description 会**一起出现在 agent 的可用技能列表里**，agent 会选错。这比版本混乱更糟。

因此：实验版平时**不装**；要测时才装成另一个名字（如 `agent-project-workflow-exp`）。

### 10.3 分叉与合并纪律

实验版从稳定版**分叉**出去，验证成功后再合并回来。禁止把两个版本写成两套独立代码——那会变成「一个事实两个家」，改一边忘另一边。

### 10.4 本版（v1.0.0）范围

**只做新建模式。** 增量模式是 v2 的候选，等稳定版实际用过、确认好用之后再启动。

---

## 附录 A · 采访决策记录

| # | 决策点 | 选定 | 理由 |
|---|---|---|---|
| 1 | 适用边界 | 仅全新项目从零开始 | 逻辑最清晰；已有项目分支会引入覆盖风险 |
| 2 | 强制程度 | 硬门禁 | 软建议等于没约束 |
| 3 | 交付物 | 完整架构 + docs/ 分层 | 用户选择；五份规则文件结构固定 |
| 4 | 采访机制 | A：记录作 SPEC 附录，**上限 5 轮** | 轮数封顶不会没完没了；读 SPEC 时顺手看到「为什么」 |
| 5 | 每轮节奏 | 每轮 3–5 问（上限不强制满）+ 第 3 轮出功能清单草稿 | 选项式提问信息密度高；草案比抽象问答高效 |
| 6 | 每轮必做 | 复述理解请用户纠正 | 一次复述挡掉的问题比多问 5 个还多 |
| 7 | 验收标准 | 先写可观测行为断言，真用一阵再补 evals | 用例质量取决于真实失败样本，现在建等于猜 |
| 8 | 名字 | `agent-project-workflow` | 全局可用。原为 `project-kickoff`，2026-09-19 改名（见 §0 变更记录） |
| 9 | 附带资源 | 带模板（`assets/`），不带脚本 | 结构统一；避免为确定性动作引入可执行代码 |
| 10 | 语言 | 中文（skill 与产出文档） | 符合现有习惯。跨平台代价见 §9.5 |
| 11 | 门禁解除 | 必须明确确认词 + 记入 SPEC 确认记录 | 防「用户没反驳 = 同意」 |
| 12 | 门禁范围 | 允许建目录、`git init`、写白名单内文件 | 文档要落盘才好讨论；其余必须等确认 |
| 13 | 提问形式 | 选项式（选项 + 推荐 + 可自由填写） | 回答快，不易漏边界 |
| 14 | 跨会话状态 | 项目内 `docs/KICKOFF-STATUS.md` | 换会话不重复采访、不误判已确认 |
| 15 | docs 分层 | 固定建全部 5 个 | 结构统一；空壳标「待填」并排进 PLAN |
| 16 | 生成顺序 | SPEC → PLAN → AGENTS.md → INDEX.md → docs/ | AGENTS.md 要引用 SPEC 条目，倒写会悬空 |
| 17 | evals 时机 | 阈值主动提议（3 次运行 / 2 次绕过） | 不打扰用户，又不会永远不补 |
| 18 | git | skill 不管 git | 用户自己处理提交节奏 |
| 19 | 触发边界 | 只含「我想做个 X」「帮我开个新项目」 | 「给现有项目加模块」从触发条件移除，避免覆盖已有文档 |
| 20 | 门禁点 | 两道：SPEC 确认 + PLAN 确认 | PLAN 写歪没人拦，会一路错到底 |
| 21 | 增量模式 | 本版不做，留作 `experimental` 分支 | 用户担心盲目加增量会把好用的新建模式搞坏 |
| 22 | 版本管理 | git 仓库 + `main`/`experimental` 分支 + tag | 稳定版先跑通；实验版从稳定版分叉，成功再合并 |
| 23 | 跨平台 | 能力无关的指令 + 一份平台说明 | 写死工具名会让其他用户跑不起来；没验证过的适配是猜 |
| 24 | 分发 | git 仓库为主 + `.skill` 包 | 兼顾版本管理与易安装 |
| 25 | description | 中英双语关键词 | 英文用户 "start a new project" 也要能触发 |
| 26 | 产出文档语言 | **固定中文**（明知与跨平台目标冲突） | 用户选择；作为已知代价记录在 §9.5 |
| 27 | 确认词 | 必须**指向对象**（「SPEC 确认」） | 堵住「OK」「没问题」这类无对象短句 |
| 28 | SPEC 完整度 | 三件套不得整节为空 | 防止带着空框架过关，否则门禁白设 |
| 29 | 执行中变更 | **一律回到门禁一** | 门禁不能只在开工前有效 |
| 30 | 文件边界 | 白名单制（目录 + `*.md` + `.gitignore` + `LICENSE`） | 「源码」一词太模糊，会被 agent 自行解释 |
| 31 | 依赖边界 | 不限于包管理器，含一切改动系统/全局环境的命令 | `nvm use` / `apt install` 同样是改环境 |
| 32 | 退出规则 | 任何阶段可说停止，立即停手且不删已生成文件 | 原 SPEC 没有退出路径 |
| 33 | 目标目录 | 前置检查前先确认目标目录 | 避免在父目录误拒 |
| 34 | 采访草稿 | 只在对话里展示，不落盘 | 落盘会与最终 SPEC 形成两个来源 |
| 35 | 仓库名与远端 | `agent-workflow`（public，`IsANineBug` 名下） | 名字涵盖未来更多流程 skill；提交身份只设本地，不动全局配置 |

---

## 附录 B · 待确认事项与已知限制

1. **增量模式**已从本版移除（决策 21）。原「覆盖现有项目需单独做个增量 skill」的记录作废——将来是从 `main` 分叉 `experimental` 分支，不是另起一个 skill。
2. **观察记录的阈值**（3 次运行 / 2 次绕过）是暂定值，用一段时间后可调。
3. **文档体积上限**（AGENTS.md ≤ 350 行、INDEX.md ≤ 300 行、docs 每个 ≤ 200 行）是起点值，按实际项目调整。
4. **硬门禁不是技术强制**（§5.8）。它依赖 agent 遵守指令。缓解办法是要求 agent 放行时明确说出「现在放行」。
5. **平台说明目前只有 DSH 一条**（§9.2），其余平台标注「未验证」。需要第二个平台实际跑过之后才补。
6. **观察记录的写入位置**尚未定死具体路径——原则是「用户可写位置，且不可写时静默降级」。实际路径在写 SKILL.md 时按平台能力决定。
7. **产出文档固定中文**（§9.5）。英文用户能触发但拿到中文文档。这是明确接受的取舍；若要服务英文用户需做本地化，属 v2 候选。
8. **README 待写**。仓库已建但暂无 README——现在还没有可描述的东西，等 skill 做出来再写，避免写一份会漂移的介绍。
9. **§5.6 已被绕过一次**（2026-09-19 16:48，改名）。它在**第一次实际使用**时就被用户主动选择跳过，说明"开工后任何变更都走两道重新确认"太重。v1.3 应重新设计这条规则，候选方向：
   - 按变更影响面分档：只改文案/命名 → 只重确认 PLAN；改功能/边界/验收标准 → 走完整两道
   - 判据必须客观可查，不能由 agent 自行判断"这算小改"
   - 无论哪档，都必须在 §0 变更记录里留痕（本次已留）
10. **PLAN 的步骤顺序缺陷**（2026-09-19 执行中发现）：v1.0.0 的 PLAN 把打 tag（S25）排在安装验证（S26/S27）**之前**，导致产物带着「⬜ 待验证」标记就发布了，不得不补发 v1.0.1。**正确顺序是「安装 → 验证 → 打 tag」。** 已同步修复 `assets/PLAN.md.tmpl`：发布/打包类步骤必须排在验证步骤之后。
11. **已发布的版本不删除**。v1.0.0 保留在 Release 列表里 —— 它记录了「先发版后验证」这个真实错误；删掉等于抹掉证据，而 v1.0.1 的说明还引用着它。标准做法是用新版**取代**旧版，不是让旧版消失。
12. **v1.1.0 修复补录**（2026-09-19 复验时补进本 SPEC，用户豁免两道重新确认）。v1.1.0 实际改了 §1/§6/§8.2/§10/§11 与 3 个模板、修掉 11+2 条缺口，但当时只记在 `PLAN.md` S19 与 `ACCEPTANCE.md`，SPEC 漏记 —— 违反「SPEC 是唯一事实来源」。本次补记：
    - **13 条缺口**：G-A/G-M/G-J（高）、G-B/G-G/G-H/G-L/G-E/G-K/G-I（中）、G-F（低）+ 自检发现的 G-K 同类问题（G-N 两个模板修复）。修法全文见 `ACCEPTANCE.md`。
    - **SKILL.md 体积上限 300 → 350**（现 345/350）：修复正当瘦身（外移 `references/platforms.md`）后仍超，显式调整并留痕于 `PLAN.md` S19；后续纪律：再增长必须先外移到 `references/`，不得继续抬上限。
    - **复验结果（2026-09-19）**：G-A 第一轮暴露新缺口 **G-O**（§1「停手」的宾语不明，agent 把「你看着办」当成 skill 外开工授权），修复后复跑通过；G-M/G-J 一次运行同时复验通过；E2E-2 补测通过。**E2E 总成绩 19/20**（E2E-20 环境无法模拟，非缺陷）。全文见 `ACCEPTANCE.md` 复验节。

---

**状态**：PLAN 27 步全部执行完毕。skill 已发布 v1.0.1 并安装到 `~/.dsh/skills/agent-project-workflow/`，运行时技能目录已刷新识别。
