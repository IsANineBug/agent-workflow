# PLAN — project-kickoff skill 实施计划

## 0. 元信息

| 项 | 值 |
|---|---|
| 依据 | `SPEC.md` v1.2（**已确认**，原话「spec确认」，2026-09-19 16:12:29 CST） |
| 版本 | v1.0（待确认） |
| 状态 | **未确认** — 门禁二未放行 |
| 步骤数 | 27 步，5 个阶段 |
| 提交纪律 | **每步一次 git 提交**，提交信息写"决策"而非"编辑" |
| 依赖安装 | **本计划无任何装依赖步骤** |

### 确认状态

| 门禁 | 状态 | 确认原话 | 时间 |
|---|---|---|---|
| 门禁一 · SPEC | ✅ 已放行 | spec确认 | 2026-09-19 16:12:29 CST |
| 门禁二 · PLAN | ⬜ **未放行** | （待填） | （待填） |

---

## 1. 两个需要你一并定的决策点

这两件事在 SPEC 里没有定死，卡在实施路径上，请在看本 PLAN 时一并决定。

### D1 · 观察记录写在哪

SPEC §9.1 要求「用户可写位置，不可写时静默降级」，但没定具体路径。本 PLAN 的提议：

```
① <skill 目录>/observations.jsonl        ← 首选，记录跟着 skill 走
② ~/.project-kickoff/observations.jsonl  ← skill 目录只读时退回用户主目录
③ 都不可写 → 静默跳过，结尾一句话说明
```

**为什么这样排**：① 让记录跟着 skill 走，换机器时一起带走；② 用户主目录几乎总是可写，所以实际很少走到 ③。

### D2 · 门禁二在本项目其实没有约束力

按 SPEC §5.4 的白名单，**本项目全部产出都是 `.md` 文件**——SKILL.md、模板、references 全在白名单内。也就是说，即使 PLAN 没确认，门禁二也拦不住我写这些文件。

这是个真实的规则漏洞：**纯文档类项目会从门禁二下面滑过去。**

本 PLAN 的提议（两个选项，二选一）：

| 选项 | 做法 | 代价 |
|---|---|---|
| **A（推荐）** | 本 PLAN 自我加严：把「SKILL.md 与模板的**实际内容**」视同功能代码，PLAN 确认前只建目录不写内容 | 不改进 SPEC，漏洞仍在，下次遇到纯文档项目还得靠自觉 |
| B | 改 SPEC，把「文档内容」也纳入门禁二 | 改了 SPEC 就要**重新确认**，多一轮 |

**我倾向 A**：先把 skill 做出来，这个漏洞记进 v1.3 的待办，等真遇到第二个纯文档项目再改规则——现在改属于为一次性的情况增加永久复杂度。

---

## 2. 阶段 A · 骨架与模板（S01–S07）

### S01 · 建目录骨架

- **做**：创建 `project-kickoff/`、`project-kickoff/assets/`、`project-kickoff/assets/docs/`、`project-kickoff/references/`
- **完成后可见**：`find project-kickoff -type d` 输出 4 个目录路径

### S02 · 写 `assets/SPEC.md.tmpl`

- **做**：产出 SPEC 模板骨架
- **完成后可见**：模板文件含 5 个必备区块——功能清单、明确不做的事、验收标准、确认记录表、采访决策附录；外加元信息表与两道门禁的确认记录

### S03 · 写 `assets/PLAN.md.tmpl`

- **做**：产出 PLAN 模板骨架
- **完成后可见**：含确认状态区、每步的「完成后可见」字段、以及「每步只做一件事」的规则说明

### S04 · 写 `assets/AGENTS.md.tmpl`

- **做**：产出常驻规则模板
- **完成后可见**：含优先级裁决顺序、硬约束、安全红线、文档写作纪律；文件内自述体积上限（≤ 350 行）与**为什么必须小**的理由

### S05 · 写 `assets/INDEX.md.tmpl`

- **做**：产出入口索引模板
- **完成后可见**：含文件清单表（每行有「什么时候该读它」）、关键词别名表、搜索配方；**零硬编码行号**

### S06 · 写 `assets/KICKOFF-STATUS.md.tmpl`

- **做**：产出流程状态模板
- **完成后可见**：含采访轮次、SPEC 状态、PLAN 状态、下一步、是否已放弃五栏

### S07 · 写 `assets/docs/` 五个模板

- **做**：产出 `GIT.md.tmpl`、`DEPS.md.tmpl`、`SECURITY.md.tmpl`、`BUILD.md.tmpl`、`QUALITY.md.tmpl`
- **完成后可见**：5 个文件都存在；每个**首段**声明「我是以下事实的唯一来源」；每个 ≤ 200 行

---

## 3. 阶段 B · SKILL.md 主体（S08–S19）

> 阶段 B 的每一步都只写 SKILL.md 的**一个章节**。这样每步的产出都能单独看懂、单独回退。

### S08 · SKILL.md frontmatter + 骨架

- **做**：写 frontmatter（`name: project-kickoff`、中英双语 `description`）与章节骨架
- **完成后可见**：description 同时含中文触发词（「我想做个 X」「帮我开个新项目」）、英文触发词（"start a new project"）、**项目级产物**限定，以及不该触发的反例（加模块 / 重构 / 修 bug / 做个函数）

### S09 · §模式判定与前置检查

- **做**：写三步前置检查——先确认目标目录 → 再判定是否为空 → 再决定拒绝或进入
- **完成后可见**：明确写出「父目录非空不等于不是新项目」，避免误拒

### S10 · §采访流程

- **做**：写采访规则
- **完成后可见**：5 轮上限、每轮 3–5 问（上限非配额）、第 3 轮出草稿且**只在对话里展示**、每轮必须复述、到上限即停

### S11 · §确认词与确认记录

- **做**：写确认判定规则
- **完成后可见**：含「确认动作 + 对象」判定规则表；明确列出不算确认的清单（看过 / 没问题 / 单独 OK / 沉默）

### S12 · §两道门禁 + 文件白名单 + 依赖边界

- **做**：写门禁规则
- **完成后可见**：门禁表（允许/禁止两栏）；白名单（目录 + `*.md` + `.gitignore` + `LICENSE`）；禁止清单（含 `.json`/`.yaml`/`Makefile` 等）；系统级命令禁令（`nvm use`/`apt install` 等）；「放行时必须明说」规则

### S13 · §SPEC 完整度门禁

- **做**：写完整度检查
- **完成后可见**：4 项检查表（功能 ≥1、每条功能必有验收、不做的事 ≥1、边界 ≥1）+ 「不通过不允许请用户确认」

### S14 · §执行中变更 + 退出规则

- **做**：写开工后改需求与中途退出的处理
- **完成后可见**：变更走「改 SPEC → 重新确认 → 改 PLAN → 重新确认」5 步；退出时立即停手、**不删除**已生成文件

### S15 · §产出物生成顺序 + 模板用法

- **做**：写产出清单与顺序，说明每个模板怎么用
- **完成后可见**：顺序为 `KICKOFF-STATUS → SPEC → PLAN → AGENTS → INDEX → docs/`，并写明为什么这个顺序（AGENTS.md 要引用 SPEC 条目，倒写会悬空）

### S16 · §上下文工程纪律自检

- **做**：写自检项
- **完成后可见**：四项——行数**实测**并报数、交叉引用逐条核对、零硬编码行号、同一事实不出现在两个文件

### S17 · §观察记录与 evals 提议

- **做**：写观察记录规则（按 D1 的路径）
- **完成后可见**：含记录字段、三级写入路径与降级、**追加时统计总行数**、阈值（3 次运行 / 2 次绕过）提议一次

### S18 · §能力降级表

- **做**：写跨平台降级
- **完成后可见**：5 种能力（选项式提问 / 可写目录 / 命令行工具 / context-efficient-docs / 子 agent）各有降级路径；全文**不出现平台专有工具名**

### S19 · SKILL.md 精简审查

- **做**：通读一遍，把可以外移的细节移到 `references/`
- **完成后可见**：`wc -l` 报出实际行数；目标 ≤ 300 行。若超出，说明拆了什么到 references/

---

## 4. 阶段 C · references（S20）

### S20 · 写 `references/platforms.md`

- **做**：写平台说明
- **完成后可见**：DSH 一条标「已验证」并给出安装路径与已验证的降级行为；其余平台明确标「未验证」——**不写没验证过的适配方案**

---

## 5. 阶段 D · 自检与验证（S21–S23）

### S21 · 机械自检

- **做**：用命令逐项核对（**不写脚本文件**，符合 N-06）
- **完成后可见**：一份检查输出，逐条列出——各文件实际行数、是否含硬编码行号、是否含平台专有工具名、交叉引用是否全部有效

### S22 · 走查 20 条 E2E 场景

- **做**：对照 SPEC §7 的 E2E-1 ~ E2E-20 逐条判断 SKILL.md 是否覆盖
- **完成后可见**：一张 20 行的表，每条标「✅ 已覆盖 / ❌ 未覆盖」，未覆盖的当场补进 SKILL.md 或明确说明为什么不补

### S23 · 写 `README.md`

- **做**：写仓库首页说明（SPEC 附录 B 待办 8）
- **完成后可见**：含一句话定位、安装方法、怎么触发、目录结构、指向 SPEC.md 的链接；**不重复 SPEC 里的具体条目**（只给指针，避免两处漂移）

---

## 6. 阶段 E · 打包、安装与发布（S24–S27）

### S24 · 出 `.skill` 包

- **做**：用 `skill-creator` 的 `package_skill.py` 打包
- **完成后可见**：`dist/project-kickoff.skill` 生成，并报出文件大小

### S25 · 打 tag `v1.0.0` 并推送

- **做**：`git tag -a v1.0.0` 并 push 分支与 tag
- **完成后可见**：`git tag` 列表含 `v1.0.0`；GitHub 仓库页面上能看到该 tag

### S26 · 安装到 skill 目录

- **做**：把 `project-kickoff/` 装到 `~/.dsh/skills/project-kickoff/`
- **完成后可见**：`ls ~/.dsh/skills/project-kickoff/` 能看到 `SKILL.md`、`assets/`、`references/`

### S27 · 验证安装生效

- **做**：确认 skill 被平台识别
- **完成后可见**：新会话的可用技能列表里出现 `project-kickoff`，description 显示中英双语

---

## 7. 明确不做的事（本 PLAN 范围）

| 不做 | 为什么 |
|---|---|
| 不建 `experimental` 分支 | 增量模式是 v2，SPEC §10.4 已定本版不做 |
| 不建 evals 用例集 | SPEC §8 已定：先跑真实运行，达阈值再建 |
| 不写任何脚本文件 | SPEC N-06：本版只带模板 |
| 不装任何依赖 | 本计划全部产出是文档，不需要依赖 |
| 不写英文版文档 | SPEC N-14：产出固定中文 |
| 不动你的全局 git 配置 | 只用仓库本地身份 |

---

## 8. 风险与应对

| 风险 | 应对 |
|---|---|
| SKILL.md 超出行数目标 | S19 专门做精简审查；超了就外移到 references/，而不是硬删规则 |
| 模板与实际规则不一致 | S21 自检含交叉引用核对；S22 走查 E2E 场景会发现规则缺口 |
| push 时 TLS 抖动（已遇到过一次） | 重试 2–3 次并退避；已在建仓时验证有效 |
| 安装后 description 触发不准 | S27 只验证「能被识别」；真正的触发准确率要等 evals，属后续 |

---

**下一步**：请确认本 PLAN，并一并回复 **D1**（观察记录路径）与 **D2**（门禁二漏洞怎么处理）的选择。

按 SPEC §5.3，确认需要**指向对象**，例如说「**PLAN 确认**」。
