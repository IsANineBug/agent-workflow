# KICKOFF-STATUS — {{项目名}}

<!--
流程状态文件。回答一个问题：**现在卡在哪一步。**

为什么需要它：硬门禁依赖「确认到哪了」。这个状态如果只存在于对话历史里，
换会话或上下文被压缩后，agent 会重复采访、或误判已确认而放行。

维护规则：
- 采访**开始**时创建（早于 SPEC.md），因为它要记录采访轮次
- **每轮采访结束都更新一次**，不只在"状态变化时"更新——否则中途断线会丢进度
- 与 SPEC.md 的确认记录冲突时，**以 SPEC.md 为准**并回来修正本文件
-->

## 1. 门禁状态

| 门禁 | 状态 | 确认原话 | 时间 |
|---|---|---|---|
| 门禁一 · SPEC | ⬜ 未确认 | — | — |
| 门禁二 · PLAN | ⬜ 未确认 | — | — |

**当前允许做什么**：建目录、`git init`、写 `*.md` / `.gitignore` / `LICENSE`。
**当前禁止做什么**：写白名单外的任何文件、装依赖、改动系统或全局环境。

---

## 2. 采访进度

| 项 | 值 |
|---|---|
| 当前轮次 | 第 {{N}} 轮 / 上限 5 轮 |
| 本轮问题数 | {{n}}（上限 5，不强制问满） |
| 第 3 轮功能清单草稿 | ⬜ 未出 / ✅ 已出（只在对话里，不落盘） |
| 每轮复述 | {{最近一次复述的要点}} |

---

## 3. 已确定的关键决策

<!-- 只记结论和指针，理由写在 SPEC 的采访决策附录里，不要在这里重复。 -->

| # | 决策点 | 选定 |
|---|---|---|
| 1 | {{决策点}} | {{选定}} |

---

## 4. 下一步

{{一句话说清下一个动作是什么，以及谁需要做}}

---

## 5. 是否已放弃

| 项 | 值 |
|---|---|
| 状态 | ⬜ 进行中 / ⬜ 已放弃 |
| 放弃时间 | — |
| 放弃时停在哪 | — |

> 用户在任何阶段说「停止 / 算了 / 不做了」→ 立即停止，不再生成或修改文件，
> **不删除**已生成的文档，回到这里记一行。
