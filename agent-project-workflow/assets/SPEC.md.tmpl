# SPEC — {{项目名}}

<!--
填写指引（生成正式 SPEC 时删除本注释）：
1. 完整度硬要求：功能清单 ≥1 条、每条功能必有验收标准、不做的事 ≥1 条、适用边界 ≥1 条。
   任何一节整节为空，都不允许进入确认环节。
2. 验收标准写成「可观测的行为」：用户做什么 → 能看到什么。
   不要写「系统应支持 X」这类无法验证的话。
3. 不写行号，不指向具体文件行——行号一小时内就会失效。
4. 本文件是项目唯一的「做什么」事实来源。怎么做写进 PLAN.md，规则细节写进 docs/。
-->

## 0. 元信息

| 项 | 值 |
|---|---|
| 版本 | v1.0（待确认） |
| 日期 | {{YYYY-MM-DD}} |
| 状态 | **未确认** — 确认前不写任何源码 |

### 0.1 确认记录

> 只有同时出现「确认动作 + 对象」才算确认，例如「SPEC 确认」。
> 「看过」「没问题」「单独一个 OK」都不算，必须追问。

| 门禁 | 确认原话 | 时间 |
|---|---|---|
| 门禁一 · SPEC | （待填） | （待填） |
| 门禁二 · PLAN | （待填） | （待填） |

---

## 1. 一句话目标

{{一句话说清：这个项目解决什么问题、给谁用}}

---

## 2. 适用边界

### 2.1 应该触发 / 适用场景

| 场景 | 说明 |
|---|---|
| {{场景}} | {{补充}} |

### 2.2 不应该做 / 不适用

| 场景 | 为什么 |
|---|---|
| {{场景}} | {{原因}} |

---

## 3. 功能清单

<!-- 每条功能必须有「做什么 / 为什么 / 验收标准」三小节，缺一不可。
     验收标准写「用户做什么 → 能看到什么」，让人翻一遍就能判对错。 -->

### F-01 {{功能名}}

- **做什么**：{{一句话}}
- **为什么**：{{不做的后果，或做了的好处}}
- **验收标准**：
  - {{用户操作}} → {{能观察到的结果}}
  - {{用户操作}} → {{能观察到的结果}}

### F-02 {{功能名}}

- **做什么**：
- **为什么**：
- **验收标准**：
  -

---

## 4. 明确不做的事

<!-- ≥1 条。确实没有时，也要显式写「暂无」，并请用户确认这一句。
     没有「不做的事」的 SPEC，等于一份谁都能往里塞需求的空框架。 -->

| 编号 | 不做 | 为什么不做 |
|---|---|---|
| N-01 | {{不做的事}} | {{原因}} |

---

## 5. 端到端验收场景

<!-- 用户走一遍完整流程，应该看到什么。
     这些既是验收考卷，也是以后 evals 用例的来源。 -->

| 编号 | 用户说 / 场景 | 期望观察到 |
|---|---|---|
| E2E-1 | {{场景}} | {{期望}} |

---

## 6. 门禁规则

### 6.1 两道闸

| 门禁 | 触发条件 | 允许 | 禁止 |
|---|---|---|---|
| 门禁一 | SPEC 未确认 | 建目录、`git init`、写白名单内文件 | 生成 PLAN.md、写白名单外文件、装依赖 |
| 门禁二 | PLAN 未确认 | 写文档、调整 PLAN | 写白名单外文件、装依赖 |
| 放行 | SPEC + PLAN 均确认 | 按 PLAN 逐步实现 | 跳过步骤、擅自加功能 |

**放行时必须明说**：agent 每次放行都要说出一句「SPEC 与 PLAN 均已确认，现在开始第 1 步：X」。门禁无法技术强制，但静默放行与明说放行的差别很大——后者能当场发现违规。

### 6.2 文件白名单

门禁期间允许写什么，见 `AGENTS.md` §3（**唯一事实来源**）。本文件不复述清单 —— 两处正文必然漂移。

### 6.3 依赖边界

依赖与系统环境的禁令，见 `docs/DEPS.md` §3（**唯一事实来源**）。

### 6.4 执行中变更

SPEC 与 PLAN 都确认、已开始实现之后，用户改需求 → **一律回到门禁一**：

1. 改 SPEC（记一行变更日志）
2. 请用户重新确认 SPEC
3. 改 PLAN
4. 请用户重新确认 PLAN
5. 继续实现

已完成的步骤不重做，但要检查是否被新需求影响。

### 6.5 退出

任何阶段用户说「停止 / 算了 / 不做了」→ 立即停止，不再生成或修改文件，**不删除**已生成的文档，用一句话说明停在哪儿。

---

## 附录 A · 采访决策记录

<!-- 记录「选了什么 / 为什么」。三个月后没人记得为什么不做 X，这张表就是答案。 -->

| # | 决策点 | 选定 | 理由 |
|---|---|---|---|
| 1 | {{决策点}} | {{选定}} | {{理由}} |

---

## 附录 B · 待确认事项与已知限制

1. {{待定项}}
