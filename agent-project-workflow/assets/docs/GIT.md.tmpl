# docs/GIT.md — Git 约定

> **我是以下事实的唯一来源**：提交信息格式、提交粒度、分支命名、合并方式。
> 其他文件提到 Git 约定时**只能给指针**，不得复述本文件内容。

<!-- 上限 200 行。写完后用 wc -l 实测。 -->

## 1. 提交信息格式

```
{{类型}}: {{一句话说清这次决定了什么}}
```

| 类型 | 用于 |
|---|---|
| `feat` | 新增功能 |
| `fix` | 修复缺陷 |
| `docs` | 只改文档 |
| `chore` | 构建、配置、依赖 |
| `refactor` | 不改行为的重构 |
| `test` | 只改测试 |

## 2. 提交粒度

**提交记录「决策」，不是「编辑」。**

判据：如果两条提交信息能用「和」连起来而不别扭，它们就是一次提交。

反例（该合并成一次）：
- `fix: 修正数量`
- `fix: 重新校准表格`
- `fix: 修掉重复计数的 grep`

这三条其实是同一次审计的尾巴，应该合并成一次。

## 3. 分支

| 分支 | 用途 |
|---|---|
| `main` | 稳定版 |
| {{其他分支}} | {{用途}} |

## 4. 明确不做

- {{不做的 Git 操作，以及为什么}}
