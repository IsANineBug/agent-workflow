# docs/QUALITY.md — 质量与完成定义

> **我是以下事实的唯一来源**：性能预算、完成定义（DoD）、自检清单。
> 其他文件提到质量指标时**只能给指针**，不得复述本文件内容。

<!-- 上限 200 行。 -->

## 1. 性能预算

<!-- 预算要写成可测量的数字。写「要快」等于没写。 -->

| 指标 | 预算 | 怎么测 |
|---|---|---|
| {{指标}} | {{数字}} | {{测法}} |

## 2. 完成定义（DoD）

一个步骤算「做完了」，必须同时满足：

- [ ] 步骤描述的「完成后可见」现象已实际观察到
- [ ] 没有引入 `SPEC.md` 里「明确不做的事」
- [ ] 涉及的上限/预算已用命令**实测**并报出数字，不是凭感觉
- [ ] 新增或修改的文档里没有硬编码行号
- [ ] 同一个会漂移的事实没有出现在两个文件里

## 3. 自检清单

```bash
# 文档体积实测（不要目测）
wc -l AGENTS.md INDEX.md SPEC.md PLAN.md docs/*.md

# 硬编码行号检查
grep -rnE '见 [0-9]+(-[0-9]+)? 行|lines [0-9]+' --include='*.md' . | grep -v '绝不写行号'

# 「不做的事」是否被误实现
grep -n '^| N-' SPEC.md
```
