# docs/DEPS.md — 依赖

> **我是以下事实的唯一来源**：允许使用的依赖清单、版本约束、新增依赖的流程。
> 其他文件提到依赖时**只能给指针**，不得复述本文件内容。

<!-- 上限 200 行。 -->

## 1. 当前依赖

| 依赖 | 版本 | 为什么需要 |
|---|---|---|
| {{依赖名}} | {{版本}} | {{理由}} |

> 没有任何依赖时，也要显式写「本项目零依赖」——空着会被误读成「还没填」。

## 2. 新增依赖的流程

1. 在 `PLAN.md` 里**单独成步**，标注「需用户批准」
2. 等用户明确同意
3. 同意后才安装，并把依赖登记到 §1

**不允许**把安装混在其他步骤里顺手做掉。

## 3. 禁止的安装方式

禁止一切**改动系统或全局环境**的命令，不只是包管理器：

| 禁止 | 为什么 |
|---|---|
| `pip install` / `npm install` / `cargo add` | 改动项目或全局依赖 |
| `nvm use` / `rustup` | 改动工具链版本，会影响其他项目 |
| `apt install` / 系统包管理 | 改动整个系统 |
| 修改全局配置文件 | 影响范围超出本项目 |

## 4. 版本约束

{{约束说明，或「暂无」}}
