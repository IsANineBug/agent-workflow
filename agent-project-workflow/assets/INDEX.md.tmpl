# INDEX — {{项目名}} 文档入口

<!--
新会话第一个读的文件。回答一个问题：**东西在哪、怎么搜。**

三条纪律：
1. 绝不写行号。「见 501–602 行」一小时内就会失效——写搜索配方。
2. 绝不写「去读文档」。要告诉 agent 跑哪条命令、读哪一节。
3. 别名表是这里价值最高、也最常被漏掉的部分。文档写「结束任务」，
   用户说「kill 进程」，grep 就找不到。同义词必须显式列出来。

上限 300 行，写完用 wc -l 实测。
-->

## 1. 文档地图

| 文件 | 回答什么问题 | 什么时候该读它 | 怎么加载 |
|---|---|---|---|
| `AGENTS.md` | 动手前必须知道的规则 | **每个会话开头** | 全文（保持 ≤350 行） |
| `SPEC.md` | 做什么、不做什么、验收标准 | 要确认某功能是否存在、边界在哪 | 按节读：`grep -n '^### F-' SPEC.md` |
| `PLAN.md` | 怎么做、分几步 | 要知道下一步做什么、某步完成了没 | 按步读：`grep -n '^### S' PLAN.md` |
| `docs/KICKOFF-STATUS.md` | 现在卡在哪道门禁 | **新会话恢复进度时** | 全文（很短） |
| `docs/GIT.md` | 提交与分支约定 | 要提交、要开分支时 | 全文 |
| `docs/DEPS.md` | 允许用哪些依赖 | 想引入新依赖时 | 全文 |
| `docs/SECURITY.md` | 安全红线 | 碰到文件、网络、权限操作时 | 全文 |
| `docs/BUILD.md` | 目录结构、构建、环境 | 要跑构建、找不到文件时 | 按节读 |
| `docs/QUALITY.md` | 性能预算、完成定义 | 判断"做完了没"时 | 按节读 |

---

## 2. 关键词别名表

<!-- 左边是「人可能会说的词」，右边是「文档里实际用的词」。
     新增术语时同步更新这张表——漏了它，grep 就白搭。 -->

| 你可能搜 | 文档里实际写的是 | 在哪 |
|---|---|---|
| {{口语说法}} | {{文档用词}} | {{文件}} |

---

## 3. 搜索配方

<!-- 复制粘贴就能用。不要写行号，只写命令。 -->

```bash
# 找某个功能的规格（按编号）
grep -n '^### F-' SPEC.md

# 找某个功能在哪几节被提到
grep -n '{{功能名}}' SPEC.md PLAN.md

# 找计划里的某一步
grep -n '^### S' PLAN.md

# 读某一节（从标题往后 40 行）
read_sec() { local s; s=$(grep -n "$2" "$1" | head -1 | cut -d: -f1); sed -n "${s},$((s+${3:-40}))p" "$1"; }
read_sec SPEC.md '^### F-03' 30

# 当前卡在哪道门禁
grep -n '门禁' docs/KICKOFF-STATUS.md

# 查「不做的事」——避免实现一个被明确排除的功能
grep -n '^| N-' SPEC.md
```

---

## 4. 目录结构

<!-- 只列到能看懂"东西大概在哪"的粒度。详细结构在 docs/BUILD.md。 -->

```
{{项目根}}/
├── AGENTS.md          ← 常驻规则
├── INDEX.md           ← 本文件
├── SPEC.md            ← 做什么
├── PLAN.md            ← 怎么做
├── docs/              ← 按需读的细则
└── {{源码目录}}        ← 实现
```
