# AGENTS.md — {{项目名}}

<!--
这个文件每个会话都会被**全文加载**，而且每次编辑它都会再全文重注一次。
它是上下文预算里最贵的一块。所以：

  只放「动手之前必须知道」的东西。上限 350 行。

一周才用一次的细节（Git 约定、依赖清单、目录树、配色、构建脚本说明）
全部放 docs/，这里只留一行指针。

写完请用 wc -l 量一下实际行数，不要凭感觉。
-->

## 0. 优先级裁决

出现冲突时，按这个顺序判：

```
用户当面指令  >  SPEC.md  >  本文件  >  docs/  >  业界最佳实践
```

SPEC 是「做什么」的唯一事实来源；本文件是「怎么做」的硬约束；docs/ 是细节。

---

## 1. 硬约束

<!-- 只写不可协商的。每条后面写清「为什么」——带理由的规则活得下来，光写禁令的会被忽略。 -->

| 约束 | 为什么 |
|---|---|
| {{约束}} | {{理由}} |

---

## 2. 安全红线

<!-- 绝对不许做的事。宁可停下来问，也不要自作主张。 -->

- {{红线}}

---

## 3. 工作流门禁

| 门禁 | 状态 | 含义 |
|---|---|---|
| 门禁一 | {{✅/⬜}} | SPEC 未确认前：只允许建目录、`git init`、写 `*.md` / `.gitignore` / `LICENSE` |
| 门禁二 | {{✅/⬜}} | PLAN 未确认前：禁止写白名单外的任何文件、禁止装依赖 |

- 白名单外文件一律禁止，包括 `.json`、`.yaml`、`.toml`、`.sh`、`Makefile` 及任何语言源文件
- 禁止一切改动系统或全局环境的命令（`pip install`、`npm install`、`nvm use`、`apt install` 等）
- **放行时必须明说**：「SPEC 与 PLAN 均已确认，现在开始第 N 步：X」
- 状态记录在 `docs/KICKOFF-STATUS.md`；SPEC 的确认记录表是最终事实来源

---

## 4. 文档写作纪律

<!-- 这部分是「为什么这个文件这么小」的答案，必须留在常驻文件里，否则下一个会话会把细节又搬回来。 -->

- **一个会漂移的事实只能有一个家**。版本号、路径、数量、依赖名——写在一处，别处只放指针
- **不写行号**。行号一小时内就会失效。要定位就写搜索配方：`grep -n '^## ' 文件`
- **不告诉 agent「去读文档」**，只告诉它跑哪条命令读哪一节
- **编辑本文件要批量**。每次编辑都会重注全文，改五次就是五倍成本
- **每条规则旁边写理由**。「不要 X」会被忽略，「不要 X，因为 Y」才活得下来
- **本文件体积上限 350 行**，写完用 `wc -l` 实测并报出数字

---

## 5. 指针（细节都在这里，别搬回来）

| 想知道什么 | 去哪 |
|---|---|
| 做什么、不做什么、验收标准 | `SPEC.md` |
| 怎么做、分几步 | `PLAN.md` |
| 东西在哪、怎么搜 | `INDEX.md` |
| Git 约定 | `docs/GIT.md` |
| 允许的依赖 | `docs/DEPS.md` |
| 安全细则 | `docs/SECURITY.md` |
| 构建与目录结构 | `docs/BUILD.md` |
| 质量预算与完成定义 | `docs/QUALITY.md` |
| 当前卡在哪道门禁 | `docs/KICKOFF-STATUS.md` |
